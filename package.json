{
  "name": "35k-flight-journal",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first flight journal for a window-seat person.",
  "license": "MIT",
  "scripts": { "test": "node --test tests/core.test.cjs", "check": "node --check dist/app.js && node --check dist/core.js && node --check dist/data.js" },
  "engines": { "node": ">=18" }
}
